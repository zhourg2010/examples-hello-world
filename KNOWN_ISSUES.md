# Known Issues / 已知局限

本文件记录 nodepipe 在 v1.0 (2026-07-18) milestone 时点上，已知但尚未解决或尚未验证的事项。目的是让未来的自己（或家人）知道哪些数据是"理论推算"，哪些是"实测"，避免误判。

## 1. 节点画像维度中的"理论值" vs "实测值"

节点解析卡片（"解析"面板）里的评分维度，实际数据来源可信度不一致：

- **实测（来自 subs-check 真实测速/平台检测结果）**：速度、AI亲缘度（部分基于 Claude/ChatGPT/Gemini 实测通过结果）、连接稳定性（基于 node_cache.py 累计出现次数）
- **理论值（仅基于协议/TLS配置规则推算，未经真实抓包/审查测试验证）**：
  - GFW对抗强度
  - 保密性

这两项目前只是"协议类型 + 是否用TLS"的规则打分，**不代表真实抗审查能力**，不要用来做安全决策依据。

## 2. IP纯净度关键词匹配未经真实样本验证

"AI亲缘度"评分里用到的住宅IP/数据中心IP判断，目前是基于关键词猜测（`住宅/residential/mobile/idc/数据中心/机房/datacenter/hosting`，加一个`risk:NN`数字模式），**这套关键词是否真的匹配 subs-check 实际写入节点名的文本格式，还没有拿真实数据核对过**。如果发现"AI亲缘度"评分明显不合理，第一时间怀疑这里，而不是怀疑整体逻辑坏了。

## 3. Surge / Quantumult X / Loon 三种格式未经真机验证

这三种格式的配置语法是照着 [Sub-Store](https://github.com/sub-store-org/Sub-Store) 的 producer 实现核对的（那是这几种格式事实上的参考实现），协议支持范围也是从那里核实的（比如"Surge 不支持 vless/anytls"、"QX 和 Loon 都支持"），但**没有在真机客户端上逐个导入验证过**。clash 和 sing-box 两种格式是用真实解析器（PyYAML / sing-box 1.13.0 官方二进制）实测验证过的。

如果家人反馈这三种格式导入失败，先怀疑这里，对照 `surge.ts` / `quanx.ts` / `loon.ts` 里生成的那一行跟客户端文档核一下字段名。

## 4. V2Box 是否支持 anytls，无法核实

`/v2box` 这条链接把 anytls 节点摘掉了，但**这是保守处理，不是已证实的结论**。

V2Box 是闭源的 iOS/macOS App，翻不了源码。能确定的只有一半：Xray-core 本身完全不支持 anytls（`XTLS/Xray-core` 的 `proxy/` 目录下根本没有，全仓库 grep 零命中）。但"V2Box 只用 Xray-core"这一点无从证实，而 v2rayN 恰好是"同时带 Xray + sing-box 两个内核、按节点自动切"的反例——所以不能从"底层是 Xray"直接推出"不支持 anytls"。

对比一下：`/v2rayn` 以前也带着同样没有依据的"支持不稳定"说法并摘掉了 anytls，2026-08 对着 2dust/v2rayN 源码核实后确认那个说法是错的，已经改成全协议。V2Box 这条因为核实不了，暂时维持保守。**实测能用的话，把 `formats.ts` 里 v2box 那一处的 protocols / render 换成 `ALL_PROTOS` 即可。**

这条也是个教训：仓库里"某客户端不支持某协议"这类断言，如果没写明核实来源，就不要当成事实——`/v2rayn` 那条错误说法在代码里躺了很久，白白少推了 anytls 节点。

## 5. 速度条上限可能需要调整

节点表格里的速度进度条以 2000KB/s 作为100%上限，是当时按经验设的值，随着网络条件变化（家庭带宽升级、境外节点质量变化等），这个上限未来可能显得过低（进度条经常爆表）或过高（大部分节点看起来都很短），需要人工观察后调整。

---

# Disaster Recovery / 灾难恢复说明

如果 Deno Deploy 账号丢失、Mac mini 硬件故障，或需要从零搭建整套系统，大致恢复步骤：

## 需要重建的部分

1. **Deno Deploy 项目 + KV 数据库**
   - 代码可以从GitHub仓库重新部署（`deployctl` 或 Deno Deploy 网页界面关联仓库）。
   - KV里存的数据（订阅节点池、设备列表、访问统计 nodeStats）**没有自动备份机制**——如果Deno Deploy账号本身丢失，这部分历史数据无法恢复，只能等下一次Mac mini推送重新生成节点池；设备列表、历史访问统计会丢失，需要手动用"添加设备"重新登记各家庭设备。
   - 建议：如果重视这些统计数据，考虑手动定期导出KV快照（目前没有实现这个功能，是一个可以做的future work）。

2. **Mac mini 测速服务 (subs-check)**
   - 从GitHub仓库(或subs-check原始项目 beck-8 fork v1.6.1)重新拉取部署。
   - 需要重新配置：
     - `gen_config.sh` 里的机场订阅源信息（这是私密的，不在GitHub仓库里，需要用户自己保存的原始配置作为依据）
     - subs-check `/admin` 面板的 api-key（当前是随机生成，重启会变——建议固定写死一个，避免每次重建都要重新查日志确认key）
     - cron 定时任务 (`0 6,19 * * *`)

3. **GL.iNet / mihomo 透明代理配置**
   - 路由器侧配置目前不在本仓库版本控制范围内，需要用户自己保留一份配置备份（导出mihomo配置文件），否则路由器损坏时需要从头搭建透明代理规则。

## 恢复时的验证顺序建议

1. 先确认 Deno Deploy 服务本身能访问（`/admin`面板、`/l/{user}/{id}/{format}`订阅链接是否正常返回）。
2. 再确认 Mac mini 测速+推送链路是否把新节点池成功推到 Deno KV（看"状态"页的批次时间戳/更新时间是否刷新）。
3. 最后确认各设备客户端能正常拉取到对应格式的订阅内容。

